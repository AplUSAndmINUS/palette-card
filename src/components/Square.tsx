import React from 'react';

const Square = () => {
  const squareStyles = {
    height: '300px',
    backgroundColor: '#FF6663'
  };

  return <div style={squareStyles}> </div>;
};

export default Square;
